// ASCEND — Scene: onboarding
// Wireframe reference: design/wireframes/onboarding.html
// Full implementation: task 1.8
9

import type { GameState } from '../core/state';

export async function mount(_container: HTMLElement, _state: GameState, _payload?: unknown): Promise<void> {
  throw new Error('Scene onboarding not yet implemented');
}

export async function unmount(): Promise<void> {
  // Clean up DOM, remove event listeners
}
